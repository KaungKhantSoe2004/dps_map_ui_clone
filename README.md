First, I initialized a React project using Vite. Then, I installed Tailwind CSS to handle the styling of the application. After that, I inspected the original website using the browser’s developer tools and extracted the necessary assets, such as CSS files and images. Next, I copied the HTML structure of home page and products page and used DeepSeek AI to convert it into React components. Finally, I refined and adjusted the styles as needed.
